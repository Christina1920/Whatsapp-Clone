import React from "react";
import { Text, View } from "react-native";

const Status = () => {
return (
	<View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
	<Text style={{ color: "#006600", fontSize: 30 }}>Status!</Text>
	</View>
);
};

export default Status;
