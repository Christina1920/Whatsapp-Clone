import React from "react";
import { Text, View } from "react-native";

function Chat({ navigation }) {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text style={{ color: "#006600", fontSize: 30 }}>Chat Screen</Text>
      </View>
    );
  }
export default Chat;
